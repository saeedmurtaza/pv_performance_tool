# Filter for Seoul locations
seoul_df = df[df['Step 1'] == 'Seoul Metropolitan City'].copy()

# Construct full location name
seoul_df['name'] = seoul_df[['Step 1', 'Step 2', 'Step 3']].fillna('').agg(' '.join, axis=1).str.strip()

# Extract coordinates in decimal degrees
seoul_df = seoul_df[['name', 'Hardness (seconds/100)', 'Latitude (seconds/100)']]
seoul_df.columns = ['name', 'longitude', 'latitude']

# Drop rows with missing or invalid coordinates
seoul_df = seoul_df.dropna().drop_duplicates()

# Show to user
import ace_tools as tools; tools.display_dataframe_to_user(name="Seoul Location Suggestions", dataframe=seoul_df)
